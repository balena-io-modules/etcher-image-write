language: node_js
before_install:
  - npm install -g npm@latest
install:
  - npm install --global electron
  - npm install
script:
  - xvfb-run --server-args=$XVFB_ARGS npm test
node_js:
  - "6"
notifications:
  email: false
